#include <stdio.h>
#include <math.h>

int main() 
{
    double a, b, c;

    printf("Entrez le coefficient a (a ≠ 0): ");
    scanf("%lf", &a);
    printf("Entrez le coefficient b (b ≠ 0): ");
    scanf("%lf", &b);
    printf("Entrez le coefficient c (c ≠ 0): ");
    scanf("%lf", &a);
    if (a == 0) ; 
    {
        printf("Le coefficient a ne peut pas être égal à zéro.\n");
        return 0;
      }
